# Supabase externo.
# Valores VITE_* são públicos e devem ficar em .env.production/.env.local, não no cofre de secrets do Lovable.
VITE_SUPABASE_MODE=mock
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Google Identity Services (Web Client ID público).
# Este valor termina em .apps.googleusercontent.com e pode ser incorporado ao bundle.
# NÃO coloque o Google Client Secret no frontend ou no repositório.
VITE_GOOGLE_CLIENT_ID=

# MOBI Ticket — chave pública de instalação da origem tempo_pelotas.
# O valor termina no browser e NÃO é credencial administrativa. A segurança
# efetiva combina essa chave com source ativo + allowlist explícita no MOBI Core.
# Nunca versionar o valor real no Git.
# Sem esta variável, o portal preserva temporariamente o loader legado P0.
VITE_MOBI_TICKET_WIDGET_TOKEN=

# Compatibilidade temporária com projetos que ainda usam a anon key legada.
VITE_SUPABASE_ANON_KEY=

# Overrides server-side opcionais para hosts que aceitam variáveis próprias.
# No Lovable, use o prefixo MOBI_ porque SUPABASE_* é reservado pela plataforma.
MOBI_SUPABASE_MODE=
MOBI_SUPABASE_URL=
MOBI_SUPABASE_PUBLISHABLE_KEY=

# Chave administrativa exclusiva do servidor. Nunca use prefixo VITE_* ou NEXT_PUBLIC_*.
MOBI_SUPABASE_SECRET_KEY=

# Allowlist opcional da moderação histórica interna. Somente servidor.
# Separe múltiplos e-mails por vírgula, ponto e vírgula ou quebra de linha no ambiente real.
# Deixe vazio para manter a Moderação V1 desabilitada/fail-closed.
MOBI_PORTAL_ADMIN_EMAILS=

# Compatibilidade com outros provedores de hospedagem.
SUPABASE_MODE=mock
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Protege rotas de captura, preenchimento do arquivo, snapshots editoriais e resumo diário.
# Use um valor longo, aleatório e exclusivo do ambiente de produção.
CRON_SECRET=

# O secret TEMPO_PELOTAS_CRON_SECRET do GitHub Actions deve conter exatamente o mesmo valor de CRON_SECRET.

# Web Push. Gere um único par VAPID e mantenha a chave privada somente no servidor.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:portaltempopelotas@gmail.com
# Segredo separado para disparos administrativos manuais.
PUSH_ADMIN_SECRET=

# INMET por Gmail -> Web Push usa o App Connector Gmail já conectado no Lovable.
# LOVABLE_API_KEY e GOOGLE_MAIL_API_KEY são injetadas/gerenciadas pelo Lovable em runtime.
# Não criar OAuth próprio, refresh token, Pub/Sub ou variantes VITE_* para essa integração.
# Enquanto o Web Push público estiver suspenso, mantenha false. A rota retorna sucesso/skipped
# sem consultar o Gmail nem tentar entregar notificações.
INMET_GMAIL_PUSH_ENABLED=false

# Opcional: habilita a redação editorial meteorológica persistente com Gemini no servidor.
# O Gemini NUNCA é chamado por páginas públicas, loaders, refresh, feed, pelotas.json ou push diário.
# A única entrada autorizada é o endpoint protegido /api/cron/push-daily?task=weather-ai.
# O workflow executa às 23:00, 05:00, 11:00 e 17:00 em Brasília.
# Antes de chamar IA, o job compara o fingerprint material e reutiliza texto compatível quando possível.
# Há também um teto financeiro mensal atômico; o padrão é 150 tentativas reais de API por mês.
# Se o contador não puder ser validado ou o teto for atingido, a chamada é bloqueada e o portal segue determinístico.
# Sem chave, sem Supabase admin ou sem snapshot compatível, o portal usa o resumo determinístico.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
GEMINI_WEATHER_ENABLED=true
GEMINI_WEATHER_MONTHLY_CALL_LIMIT=150

# REDEMET/DECEA — todas as variáveis permanecem somente no runtime de servidor.
# Nunca use REDEMET_API_KEY com prefixo VITE_ e nunca versione o valor real.
# Radar operacional preferencial para Pelotas: Santiago (sg).
# Canguçu (cn) é mantido pelo código como alternativa automática quando voltar a fornecer imagem válida.
# Consulte docs/REDEMET_OPERATIONS.md antes de alterar área, produto ou contrato da API.
REDEMET_API_KEY=
REDEMET_API_BASE_URL=https://api-redemet.decea.mil.br/
REDEMET_RADAR_AREA=sg
REDEMET_RADAR_PRODUCT=maxcappi

# Defesa Civil RS — Rede de Monitoramento Hidrometeorológico.
# A integração pública fica habilitada por padrão e preserva atribuição institucional.
# Defina explicitamente false somente como kill switch operacional server-side.
# Não criar variante VITE_* desta flag.
DEFESA_CIVIL_HYDRO_ENABLED=true

# Domínio público oficial. Canonical, sitemap e dados estruturados permanecem fixos neste host.
VITE_SITE_URL=https://tempopelotas.com.br
